# resource

The `resource` crate contains macros for statically including assets in release mode, but dynamically loading them in debug mode.

**Archived in favor of https://git.sr.ht/~mistodon/resource**
